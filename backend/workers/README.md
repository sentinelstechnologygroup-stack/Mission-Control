# Phase A backend/workers

Worker-specific extraction staging area for Mission Control v0.2.

Phase A scope:
- preserve current runtime behavior
- do not introduce new worker-loop semantics yet
- reserve this folder for later runtime launcher / loop decomposition
